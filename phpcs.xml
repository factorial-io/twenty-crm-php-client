<?xml version="1.0"?>
<ruleset name="Twenty CRM PHP Client">
    <description>PSR-12 coding standard for Twenty CRM PHP Client</description>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>

    <!-- Use colors in output -->
    <arg name="colors"/>

    <!-- Parallel processing -->
    <arg name="parallel" value="8"/>

    <!-- Paths to check -->
    <file>src</file>
    <file>tests</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/var/*</exclude-pattern>
    <exclude-pattern>*/src/Entity/*</exclude-pattern>
    <exclude-pattern>*/src/Collection/*</exclude-pattern>

    <!-- Include PSR-12 coding standard (modern PHP) -->
    <rule ref="PSR12"/>

    <!-- Relax line length limit for generated code and long strings -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="200"/>
        </properties>
    </rule>
</ruleset>
